<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>Header</title>
    <link rel="stylesheet" type="text/css" href="http://getbootstrap.com/2.3.2/assets/css/bootstrap.css"></link>
    <!--<link href="grid.css" rel="stylesheet">-->
    
  </head>

  <body align="center" class="span6" onload="display(); quotePicker()">

    <div align="center" class="container span6">
    </div>

    <div align="center" class="container span6">
      <div class="page-header">
        <h1><img src="http://i.picresize.com/images/2013/11/09/L0awx.png"/> Meal Maniac <a href="settings">
          <img src="http://files.softicons.com/download/system-icons/web0.2ama-icons-by-chrfb/png/96x96/Settings.png" height="20" width="20" align="right" class="img-responsive" alt="Responsive image">
        </a></h1>
      </div>

      <h1 id="quote">  </h1>
      <h3 id="rname"></h2>
      <h3 id="rphone"> </h3>
      <h3 id="dish"> </h3>
      <h3 id="price"></h3>

      <form id = "approval" align="center" action ="order">
        <div>
          <input type="image" src = "http://upload.wikimedia.org/wikipedia/commons/1/13/Facebook_like_thumb.png" height="50" width="50" onclick="like()">
          <input type="image" src = "http://upload.wikimedia.org/wikipedia/commons/thumb/b/b1/Not_facebook_not_like_thumbs_down.png/512px-Not_facebook_not_like_thumbs_down.png" height="50" width="50" onclick="dislike()">
        </div>
      </form>
    </div>    
    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"></script>
    <script >
      function display() {
        var order = JSON.parse(localStorage.getItem("order"));
          if (order["core"].length==undefined) {
            document.getElementById('dish').innerHTML=order["core"]["name"];
            document.getElementById('rname').innerHTML=order["core"]["rest"]["name"];
            document.getElementById('rphone').innerHTML=order["core"]["rest"]["phone"];
            document.getElementById('price').innerHTML="$"+(parseInt(100*order.total))/100.0;;
          }
          else {
            document.getElementById('dish').innerHTML=order["core"][0]["name"];
            document.getElementById('rname').innerHTML=order["core"][0]["rest"]["name"];
            document.getElementById('rphone').innerHTML=order["core"][0]["rest"]["phone"].substring(0,order.total.indexOf(".")+2);
            document.getElementById('price').innerHTML="$"+(parseInt(100*order.total))/100.0;;
          }
      }
      function quotePicker(quote) {
        var lameness = localStorage.getItem("lameness");
        if (lameness==0) {
          document.getElementById("quote").innerHTML="<font color='red'> You are a TRUE maniac! </font>";
        }
        else if (lameness==1) {
          document.getElementById("quote").innerHTML="<font color='gold'>Come on, you just had to hit the red button!</font>";
        }
        else if (lameness==2) {
          document.getElementById("quote").innerHTML="<font color='blue'>Really now? If you actually /font>";
        }
      }
    </script>
    <script>
      //DISPLAY OPTIONS BY MODIFYING ABOVE DIVS
    </script>
    <script>
      form = document.getElementById("approval");
      function like() {
        //Do something. Make priority.
        
      }
      function dislike() {
        //Do something. Lower priority.
      }
    </script>
  </body>
</html>